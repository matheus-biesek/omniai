{
  "name": "omniai-e2e",
  "private": true,
  "version": "0.0.0",
  "description": "Testes ponta a ponta do OmniAI contra o sistema rodando via docker compose.",
  "type": "module",
  "scripts": {
    "test": "node --test --test-concurrency=1 --test-reporter=spec"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@microsoft/signalr": "^10.0.11",
    "playwright-core": "^1.63.0"
  }
}
